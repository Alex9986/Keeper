import React from "react";
import HighlightIcon from "@mui/icons-material/Highlight";

function Header() {
  return (
    // Rendering the header element that appears at the top of the application
    <header>
      <h1>
        <HighlightIcon />
        Keeper
      </h1>
    </header>
  );
}

export default Header;
